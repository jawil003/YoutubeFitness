import { InputProps } from "@material-ui/core";
import React from "react";

/**
 * An TimeStampConverter React Component.
 * @author
 * @version 0.1
 */
const TimeStampConverter: React.FC<InputProps> = ({
  inputComponent,
}) => {
  return <div></div>;
};

export default TimeStampConverter;
